#include "main.h"
/**
 * _islower - check for lower char
 * @c : intger val
 * Return: 1 for lower 0 for otherwish
*/
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
	return (1);
	}
	return (0);
}
